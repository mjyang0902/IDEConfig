return{
    "nvzone/showkeys",
    cmd = "ShowkeysToggle",
    opts = {
        timeout = 3, -- in secs
        maxkeys = 3,
        -- bottom-left, bottom-right, bottom-center, top-left, top-right, top-center
        position = "top-right",
    }
}

